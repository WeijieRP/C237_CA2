<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Edit Student</title>
</head>
<body>
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Interest Group</a>
    </div>
</nav>

<div class="container mt-4">
    <h2 class="text-center">Edit Student</h2>

    <form action="/students/edit/<%= student.id %>" method="POST" enctype="multipart/form-data" class="w-50 mx-auto mt-4">
        <div class="mb-3">
            <label for="id">Student ID:</label>
            <input type="text" id="id" name="id" value="<%= student.id %>" class="form-control" readonly>
        </div>

        <div class="mb-3">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= student.name %>" class="form-control" required>
        </div>

        <div class="mb-3">
            <label for="school_id">School ID:</label>
            <input type="number" id="school_id" name="school_id" value="<%= student.school_id %>" class="form-control" required>
        </div>

        <div class="mb-3">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= student.email %>" class="form-control" required>
        </div>

        <div class="mb-3">
            <label for="interests">Interests:</label>
            <textarea id="interests" name="interests" class="form-control" required><%= student.interests %></textarea>
        </div>

        <div class="mb-3">
            <label for="profile_pic">Profile Picture:</label><br>
            <% if (student.profile_pic) { %>
                <img src="<%= student.profile_pic %>" alt="Current Pic" width="100" class="img-thumbnail mb-2"><br>
            <% } %>
            <input type="file" id="profile_pic" name="profile_pic" accept="image/*" class="form-control">
        </div>

        <button type="submit" class="btn btn-primary">Update Student</button>
    </form>
</div>
</body>
</html>
