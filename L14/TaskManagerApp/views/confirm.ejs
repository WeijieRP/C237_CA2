<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>confirmations</title>
</head>
<body>
    <style>
        table , th , td{
            border:1px solid black;
        }
    </style>
 <%- include('partials/header') %>

    <h1>Submitted Task</h1>
    <table border="1" width="80%">
        <tr style="background-color: black;color: white;">
            <th>#</th>
            <th>Title</th>
            <th>Description</th>
            <th>Deadline</th>
            <th>Priority</th>
        </tr>
        <% for(let i=0;i<taskdetails.length;i++){%> 
            <tr>
    <td><%= taskdetails[i].id %></td>
    <td><%= taskdetails[i].title %></td>
    <td><%= taskdetails[i].description %></td>
    <td><%= taskdetails[i].deadline %></td>
<td><button class="m-4  btn btn-<%= taskdetails[i].priorityColor() %>"><%= taskdetails[i].priority %></button></td>
</tr>

            <%} %>
        
    </table>
</body>
</html>